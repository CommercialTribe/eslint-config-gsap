# eslint-config-gsap

This package provides global declarations for Greensock animation library.

# Usage

### 1. Install

`yarn add eslint-config-gsap --dev`

`npm install eslint-config-gsap --dev`

### 2. Update eslint config

Add `"extends": "gsap"` to your `.eslintrc`
